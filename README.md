Teleparty Video Metadata Fetcher


Features

Fetch video metadata from a given video URL.

Display BIF (thumbnail) URLs and other metadata.

Fully built with Kotlin and Retrofit for network calls.

Notes

The video URL is hardcoded in the app, so you do not need to enter anything manually.

Ensure the cookie used in the code is valid; otherwise, the API request may fail.
